/**
 * Helper function that takes the results of this plugin (pages) and 
 * creates menu configuration for the default Vuepress 2 theme
 */
/**
 * 
 * @param {Array} pages Array of pages returned from plugin
 */
export function toSidebarConfig(pages) {

}